{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build with CMake",
      "options": {
        "cwd": "${workspaceRoot}"
      },
      "command": "cmake --build build"
    }
  ]
}
